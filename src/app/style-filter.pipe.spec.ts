import { StyleFilterPipe } from './style-filter.pipe';

describe('StyleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StyleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
